{
  "name": "hoppytech",
  "private": true,
  "workspaces": ["legacy"],
  "scripts": {
    "dev": "npm run dev -w legacy",
    "build": "npm run build -w legacy",
    "lint": "npm run lint -w legacy",
    "preview": "npm run preview -w legacy"
  }
}
